/* eslint-disable no-alert */
export default function makeUpdateTarget() {
  return () => {

  };
  /*
  return (dispatch, getState) => {
    const target = window.prompt(
      'Enter your new target, in Echoes',
      getState().persistence.target || 0,
    );
  };
  */
}
